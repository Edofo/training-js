// Exercice 2.1 : Manipulation de chaînes
// Objectif: Créez une fonction inverserMot(mot) qui retourne un mot à l'envers (ex: "hello" → "olleh")


if (typeof module !== 'undefined' && module.exports) {
    module.exports = { inverserMot };
}
